[project]
name = "game-info"
version = "0.1.0"
description = "遊戲評分與簡介查詢（IGDB），供 LLM prompt 參考"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/game_info"]
