[project]
name = "pkg-events"
version = "0.1.0"
description = "Topic 常數與事件 payload schema（對齊 docs/events.md）"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pkg_events"]
